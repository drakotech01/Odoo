<odoo>
    <data noupdate="1">
        <record id="cron_generate_default_allocations" model="ir.cron">
            <field name="name">Asignaciones predeterminadas de vacaciones y cumpleaños</field>
            <field name="model_id" ref="hr.model_hr_employee"/>
            <field name="state">code</field>
            <field name="code">model.generate_allocations_for_all_employees()</field>
            <field name="interval_type">days</field>
            <field name="interval_number">1</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(minutes=5)).strftime('%Y-%m-%d %H:%M:%S')" />
        </record>
    </data>
</odoo>
